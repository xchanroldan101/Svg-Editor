{
  "name": "Svg-Editor",
  "version": "1.0.0",
  "description": "XML Vector Workspace code editor, free, no signup, import, export SVG files, dark and light grid, view images, errors, you can download this site as a zip.",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint index.js",
    "test:coverage": "jest --coverage",
    "build": "echo 'Static HTML project. Skipping compilation steps...'",
    "deploy": "echo 'Deploying static assets to production environment...'"
  },
  "devDependencies": {
    "eslint": "^9.9.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
