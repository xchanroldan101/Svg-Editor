{
  "name": "Svg-Editor",
  "version": "1.0.1",
  "description": "This is the Svg Editor. https://github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "node version-bump.js",
    "lint": "eslint index.js",
    "test:coverage": "jest --coverage",
    "build": "echo 'Static HTML project. Skipping compilation steps...'",
    "deploy": "echo 'Deploying static assets to production environment...'"
  },
  "devDependencies": {
    "eslint": "^9.9.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
